auto break case char const continue delete do double else enum explicit float for goto if int long register return short 
signed sizeof static struct switch typedef return shotrt signed sizeof static struct switch typedef 